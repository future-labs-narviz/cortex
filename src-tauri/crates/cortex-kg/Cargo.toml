[package]
name = "cortex-kg"
version = "0.1.0"
edition = "2021"
description = "Typed knowledge graph storage for Cortex"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
log = "0.4"
specta = { version = "=2.0.0-rc.22", features = ["derive"] }
